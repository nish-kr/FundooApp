import { EmailSearchPipe } from './email-search.pipe';

describe('EmailSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new EmailSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
