import { CofirmpasswordDirective } from './cofirmpassword.directive';

describe('CofirmpasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new CofirmpasswordDirective();
    expect(directive).toBeTruthy();
  });
});
